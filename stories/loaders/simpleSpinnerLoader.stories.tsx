import * as React from 'react';

import { storiesOf } from '@storybook/react';
import { SimpleSpinnerLoader } from '../../src';

storiesOf('SimpleSpinnerLoader', module).add('default props', () => <SimpleSpinnerLoader/>);
